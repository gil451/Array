/*Conteo de elementos
Dado un arreglo de strings, retorna cuántos elementos tiene el arreglo. */

const palabras = ["hola", "Manzana", "Cuando", "Gilberto"];
const cantidad = palabras.length;

console.log(cantidad);
console.log("Las palabras ingresadas son: " + palabras.join(' ') );
